server:
  shutdown: graceful
  port: 8090
spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  datasource:
    url: jdbc:mysql://localhost:3306/popup_db?serverTimezone=UTC&characterEncoding=UTF-8&autoReconnect=true
    username: root
    password: yonguk1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create